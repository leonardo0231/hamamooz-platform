"""Archived pre-preview view module.

The active import endpoints live in :mod:`hamamooz.apps.imports.views`.
This marker is kept so old deployments that still reference the backup module
can import it without executing arbitrary expressions at module load time.
"""
